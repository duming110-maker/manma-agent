# 2026-08-18 · P0-0：dsh Host 最小启动（npm 闭包 0.1.0-rc.7）

## 决策

**启动形态**：`apps/desktop/src/main.mjs` 经 `@deepseek-ai/dsh/package.json` 的 `bin.dsh` 解析官方 CLI 公开入口，spawn `dsh web --host 127.0.0.1 --port 0`。理由：CLI 是文档化、有测试覆盖的启动面（拥有 profile boot / 信号处理 / 关停），spike 触点最小（铁律 2：唯一触点 = 包 manifest 的 bin 字段 + `dsh web` 文档化参数）。**P1 Electron 化时重新评估** CLI spawn vs 程序化 boot（评审确认可接受为 spike 形态）。

**依赖引入**：只直接钉 `@deepseek-ai/dsh: "0.1.0-rc.7"`（精确无 caret）——官方发行形态即 CLI 单入口包，dsh-base/dsh-web-app/dsh-host-webserver/dsh-web-frontend 等全部经传递依赖进入，由 `pnpm-lock.yaml` 冻结（闭包内 rc.6/rc.8 出现 0 次）。风险与对策：上游若发 rc.8，`^0.1.0-rc.7` 传递范围会漂移——**升级流程检查点：若解析漂移，加 pnpm overrides 硬钉全 scope**。`upstream.json.upgrade.verifiedVersion` 已填 `0.1.0-rc.7`，`smokeResult` 留 null（S1–S10 冒烟清单落地后回填）。

**node-pty patch 不引入**：`reference/upstream/patches/node-pty@1.2.0-beta.15.patch` 仅改 `lib/unixTerminal.js` 的 spawn-helper 解析（unix-only、asar 嵌入场景），Windows ConPTY 路径不经过它，本机 install 在无 patch 下 prebuild 直接可用（PM 与评审双重验证）。P1 Electron asar 打包时拷贝引入并实测 PTY。

**allowBuilds**（`pnpm-workspace.yaml`）：node-pty/koffi/@deepseek-ai/dsh-subprocess-local 放行、@google/genai/protobufjs 拒绝——沿用上游 workspace 的评审结论（评审逐条对照确认系正确转写而非夹带）。`minimumReleaseAgeExclude` 为 pnpm 自动生成的供应链年龄豁免（全部 `@deepseek-ai/*@0.1.0-rc.7`），**升级流程检查点：该清单只许 @deepseek-ai 钉住集，出现其它条目须人工审**。

**产品策略（PM 裁决）**：入口钉 `DSH_TELEMETRY_DISABLED=1` 为纵深防御产品策略（上游本身默认关 + fail-closed，双保险，铁律 6）。

**数据目录**：spike 落 `apps/desktop/.data/dsh-home`（`DSH_HOME` 环境变量可覆盖，已 gitignore，不落 C 盘、不落 `reference/`）。P1 迁 `%APPDATA%/<brand-id>/dsh-home` 时一并审计 DSH_HOME 之外的写入（TEMP、崩溃转储）。

## 评审附产（已采纳）

- frontend-user 的 dev 脚本原为 `vite --host 0.0.0.0`（镜像带入），与铁律 6 loopback 精神不符 → 已改 `vite --host 127.0.0.1` 并重启验证（`netstat` 仅 `127.0.0.1:3000 LISTENING`，HTTP 200）。
- 未采纳（记录）：allowBuilds 补 `node-addon-require-builtin: false`（纯记录对称性，当前 0.1.5 无生命周期脚本）；建议尽早 `git init` 恢复真 diff 门禁（仓库状态项，待用户决定）。

## 验证（PM 与评审各自独立重跑）

`pnpm install` exit 0；`pnpm --filter desktop dev` 起 Host（随机端口），`curl http://127.0.0.1:<port>/` 200 且为官方前端（`<title>DeepSeek Harness</title>`）；`netstat` 仅一条 `127.0.0.1:<port> LISTENING`（上游 web app 亦显式拒绝 `--host 0.0.0.0`，双保险）；taskkill 树杀后端口释放、无残留；`pnpm run typecheck` / `pnpm run lint` 全过；`reference/` 零改动（mtime 窗口核查）。

## P1 前置清单（遗留项）

1. node-pty patch 拷贝引入 + Windows/macOS PTY 实测；2. 优雅关停演练（Ctrl-C/SIGTERM 子树、退出码镜像、Electron Job Object 防孤儿）；3. 数据目录迁 `%APPDATA%` + DSH_HOME 外写入审计；4. S1–S10 冒烟落地后回填 `smokeResult`。
