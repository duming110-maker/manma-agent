# 2026-08-19 · P2-c：新建任务流（欢迎页 → 选工作区 → 开会话，D7 硬约束落地）

## 决策

**欢迎视图选型**：我方内容**替换主区渲染**（`data-bc-main-view` 驱动），不注入官方 `conversation.hero.*`——官方 hero 围绕 live blank 渲染，结构性无法承载「先选工作区」的 D7 前置门。boot 落点=欢迎页（docs/03 §5 权威规定 `/`=欢迎页；P2-a 的 boot=官方 hero 行为就此对齐设计，评审背书）。

**提交流通道**：`createSession` 映射官方 `IWorkspaces.connectWorkspace`（**blank 复用语义**：成员+同 cwd+未归档的 blank 直接复用，boot auto-blank 不会二次铸造——活体实证同 id 复用）→ `ISessions.open` → `promptSession` 用 `ISession.prompt(parts,'queue')`（官方 ConversationController.send 同款）。失败态回欢迎视图；业务失败（无凭证）会话照建、消息照收。

**「新建任务」不清当前选择**（PM 裁决，评审背书）：纯视图切换，避免仅点击就铸 blank（官方 `startSession` 会立即 connect+open 改变 current）；持久化重开选择保留。**这是与官方 startSession 的有意语义差异**——P2-d 导航定稿时在 docs 补一行。迁移观察器（防 bounce + blank 豁免）承担外来 open 源（boot 恢复/host 侧事件）的视图回切。

**铁律 2 升为机器纪律**：web-ui eslint `@typescript-eslint/no-restricted-imports`——`src/` 内 adapter 之外禁 `@deepseek-ai` **值** import（`allowTypeImports` 豁免 type-only；框架注入 props 属组合面的 P2-b 裁定）。评审探针双向实测（值导入报错/type 放行/adapter 豁免）。

**P2-f 台账勘误定稿**：BcShellFrame **9** + SessionList 12 + WelcomeView 11 = **32 键**（P2-b 留档的 11 系误计）。

## 评审附产（P2-d/e/f 输入）

WelcomeView 模块头「demo 欢迎页形状」措辞应改引 docs/03 §5（demo 欢迎页实为居中 hero+快捷卡，无下拉无输入——下拉+输入的规格来源是架构文档）；「无 recent 回落首行」与 docs「首次启动强制选择」措辞分歧（理论性，官方 recentWorkspaceId 恒有值）P2-f/docs 轮统一；陈旧选择瞬时怪癖留意；提交失败文案分级 P2-f 顺带。

## 验证

开发 CDP 18/18 + PM GUI 全流程亲点（欢迎页/预选/D7 门/提交→官方对话区开+首条消息渲染+侧栏立现 durable 标题/blank 复用/往返不回弹）+ 评审独立复跑 14/15（唯一 FAIL 系评审驱动自身顺序硬编码）+ eslint 边界探针 + 迁移观察器专项审查（结构上不可能死循环、无漏迁移面）三方闭环。require 面基线 `{react, react/jsx-runtime}` 不变，rev `1b66ef692cbe` 三方一致。
