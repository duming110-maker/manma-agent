# 2026-08-19 · P2-e：技能/定时任务/设置三页骨架

## 决策

**技能通道定案**：官方客户端读技能清单的文档化通道 = connection 服务的 payload-direct api 面 `ctx.connection.api.skills.list({sessionId}, signal?)`（`IApiClient.skills`，ui-skill 同款读法），adapter 增 `listSessionSkills`（含 subagent 守卫镜像）。「已安装」tab 语义 = **当前会话的技能清单**（协议耦合 sessionId 必填，P0-4 已知）；无当前会话显示耦合说明空态；「市场」空态等 P3b。

**官方 settings 嵌入否决（P2 期）**：官方设置 UI（SettingsRoot）注册进 `sidebar.settings` seat——该 seat 由 **ui-sidebar 的注册 children 声明**（one-declarer-per-slot，二次声明装载期 throw `slot is already declared`），浮层开合是组件内部 useState 无外部通道；我们 root 注册者只能 renderSlot 自己声明的 children。**「DSH 设置」= 禁用按钮 + 说明的退化形态**（评审源码级复核成立）；P3 可另立卡评估在 bc 自有 seat 下重组官方 `settings.section` 分片。

**导航扩展**：视图联合 `welcome|conversation|skills|cron|settings`；P2-c/d 两观察器逐字未动（守卫仍限 welcome/conversation）；页面切换不打断当前会话选择（实测）。**upstream face useMemo 稳定化**——开发自检发现每渲染重建致技能页 fetch 无限重发的真 bug，修复经框架缓存/依赖稳定/行为实测（mount 恰 1 次请求、tab 往返 0 额外）三层验证。

**铁律 5 违例现场修复（PM）**：SkillsPage 副标题硬编码品牌串「为 BC Agent 解锁业务能力」→ PM 改中性「为智能体解锁业务能力」并重建（产品树品牌 grep 0；rev 变化 `a6164b1d726d` 属预期）。**教训入 P2-f 卡：COPY 抽取时品牌词零容忍。**

## PM 裁决（P2-f 输入）

- **「DSH」字样**：上游产品专名（技术名词），非我方品牌——locale 字典保留专名，不进 branding/；docs/03 §5 规范用语一致。
- 36 键台账复核通过（Skills 14 + Cron 11 + Settings 11）；BcShellFrame 净增 nav 三键与三页 title 同串，按串去重。
- P3b 输入：错误态显式重试入口、fetch 失败原因保留分级、「项目级/用户级」标签（wire 无 source/rank）。
- 旧遗留：main.mjs 注释「BC Agent Desktop」在品牌 grep 口径扩到注释时清理（非用户可见）。

## 验证

开发 CDP 16/16 + 补探针 3/3 + PM GUI（三页/技能真数据/空态/回归）+ 评审 18/18 独立复跑（含技能目录跨工作区跟随、fetch 稳定性、双分支空态、rev 三方）四方闭环。require 面基线不变。
