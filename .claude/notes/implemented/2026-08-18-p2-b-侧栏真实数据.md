# 2026-08-18 · P2-b：侧栏真实数据（工作区分组 + 会话列表 + 归档）

## 决策

**官方域数据通道定案（本卡核心结论）**：官方客户端插件**不走裸 RPC**——读走 client runtime 对象层服务（`ctx.workspaces`/`ctx.sessions` 活快照 store + 变更流，组件侧经框架标准钩子 `useSessions`/`useWorkspaces`，ui-layout AppFrame 先例），写走服务方法（`ctx.sessions.open`、`ctx.workspaces.archiveSession`）。**铁律 2 的执行口径**：框架注入的 props（useSessions 等）视为组合面（同 renderSlot），我方**发起**的官方调用全部单点在 `adapters/upstream.ts`（本卡落地，含类型定义、`projectSidebarData` 投影、动作面 `createUpstreamFace`）。投影逻辑逐条移植官方 ui-workspace `tree.ts`（sessionVisible/账户序/未分组桶），评审逐字核对忠实。

**会话切换通道**：`ISessions.open(id)` 是文档化公开面（官方侧栏行点击同款），P2-b 已接——行点击后官方对话区跟随切换。**归档签名勘误**：官方契约是 `archiveSession(sessionId)` 单参（任务卡双参是笔误），D13「隐藏不删账」经账户槽位保留实证。

**上游语义留档（两条，影响产品文案/测试）**：① 会话 `blank` 标记只被 turn/start 清除（rename 不清）——纯改名的新会话不进侧栏；② runtime 启动无恢复选择时自动为最近工作区铸一条 blank「新建会话」并选为当前，切走后实时消失。

**实时性定案**：无轮询——活 store 订阅随标准钩子自动生效（host 侧 RPC 造数/归档，已打开页面实时增删行，PM 与评审双重活体实证）。

## 评审附产（P2-c/d/f 输入，PM 采纳）

1. **P2-c 落机器纪律**：铁律 2 升为 lint 边界规则（web-ui 内 `adapters/` 之外禁 `@deepseek-ai` import，eslint no-restricted-imports）。
2. 顺带修 `SessionList.tsx` 直接 type-import `SessionId`（应在 adapter 导出 `UpstreamSessionId` 别名）与 `UpstreamFeedProps` 重复声明 share 的移植纪律偏离（二选一：删交叉或注释援引）。
3. `formatRelativeTime` 补 `assertNever` 兜底（closed union 纪律）。
4. COPY 计数修正：SessionList 实为 **12 键**（自述 8 系合并计数），P2-f 清单按 11+12=23 键。
5. 视觉微差留 P2-c/d 定夺：hover 按钮 28 vs demo 32、active 圆点偏移、组头缺文件夹图标与 sticky、无 containsCurrent 组高亮。

## 验证

开发 CDP 9/9 + PM 活体（分组渲染/行点击切换/RPC 造数与归档的实时增删/archivedSessionIds 复核/相对时间分桶/暗色回归）+ 评审独立复跑（R1–R8 含未分组桶、空组渲染、blank 当前行跟随；adapter 逐字对照；铁律 2 全树 grep 9 处全 type-only；产物断言面）三方闭环。
