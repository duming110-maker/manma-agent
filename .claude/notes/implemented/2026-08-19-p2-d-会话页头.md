# 2026-08-19 · P2-d：会话页头（rename 官方通道 + 归档回切官方语义复用）

## 决策

**会话页结构定稿**：conversation 视图 = 自研页头（44px：标题就地重命名 + 工作区徽标 + 归档入口）+ 官方 conversation 原样嵌入。页头视觉按 frontend-user 页头几何，`--bc-*` 令牌。

**rename 通道**：官方文档化 `ISession.rename(title)`（`contract/session.ts:69`），adapter 逐字移植官方 ui-workspace 的 `renameSession` 包装（`binding(id)?.session.rename` → `!ok` throw）。改名经活 store 直达——页头/侧栏/官方会话导航三面同帧同步（PM GUI 实证旧标题零残留）。

**归档回切零自造状态**：官方 workspaces service `project()` 在归档落地时对「当前会话被归档」执行 `sessions.clear()`（覆盖本端回显/他 tab/重连三路径）→ current 变 undefined；我方仅以 **no-current 导航规则**（conversation + current undefined → welcome）镜像。与 P2-c 迁移观察器结构上不可互弹（二者触发条件互为对方守卫的否命题，且都不写 current）——评审专项论证 + 活体往返实测。

**徽标回落链**：所属工作区 title → cwd basename（tooltip 全路径）→「未分组」（PM 用 cwd 直创会话补测了官方未覆盖分支）。

**blank 改名 quirk 留档**：改名不清 blank 位（官方同款，`blank ? 'New Session' : displayTitle`），纯改名的 blank 会话仍不进侧栏——P2-f 考虑对 blank 会话禁用改名入口或显示草稿标题。

**docs 裁决行落地 + 评审返工项**：docs/03 §5 补「新建任务纯视图切换」裁决行；评审揪出编辑滑手产生的重复行（139 行与 138 逐字节相同）——PM 单行删除修复（评审预设接受单行 diff 确认），全文邻接重复扫描零。

## P2-f 台账（复核通过）

38 键 = BcShellFrame 9 + SessionList 12 + WelcomeView 11 + SessionHeader 6；跨模块同串（新建会话/未分组/归档）按串去重抽取；改名失败文案滞留无消隐，顺带分级。

## 验证

开发 CDP 18/18 + PM GUI（页头出现/改名三面同步/未分组徽标回落/归档回欢迎页）+ 评审（rename 逐字对照、两观察器结构论证、编辑态 settledRef 无竞态、blank quirk 官方同款核实、活体两驱动全绿）三方闭环。require 面基线不变，rev `e0c669de7b27`。
