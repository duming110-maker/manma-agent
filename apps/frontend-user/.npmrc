# BC Agent 用户端前端 —— npm 配置（对齐 frontend-admin）
# 变更记录：2026-07-10 | 初始化
# 说明：React 19 + @base-ui/react + shadcn 多包并存时偶发 peer 依赖告警，
# 开启 legacy-peer-deps 让 npm 忽略 peer 冲突，避免安装被无谓卡住。
legacy-peer-deps=true
