# 事实核查附录：方案文档主张 vs 上游源码

- 核查基准：上游 master 47f9438（2026-08-18 工作副本）
- 用途：支撑 [评审报告-2026-08-18.md](评审报告-2026-08-18.md) 的问题判定。行号以当前工作副本为准。
- 结论标记：✅ 属实 ｜ ⚠️ 部分属实/有偏差 ｜ ❌ 不属实

## 1. 会话与 RPC（01 §2.2、05 §4.1）

| 主张 | 结论 | 证据 |
|---|---|---|
| 会话 RPC：创建/列表/历史/发送/取消/重命名/导出/搜索 | ⚠️ | 方法表 `packages/host/apiproxy/src/api/rpc-map.ts:25-36`：list/search/create/history/models/selectModel/rename/fork/prompt/attachment/updateQueue/cancel。**无 `session.delete`**（全仓库零命中）；**export 不是 RPC 方法**，是 GET 下载路由 `GET /api/session.export`（`fetch/handler.ts:260-271`，返回会话日志 ZIP） |
| `session.create` 如何绑定工作目录（R11 待验证） | ✅（可直接关闭） | 接受 `workspaceId` 或 `cwd`（互斥，schema refine）：`api/sessions.ts:261-262`、`sessions.schema.ts:102-110`；handler `api-proxy.ts:2167-2240`，`cwd = workspace?.path ?? request.payload.cwd ?? defaults.cwd`（:2180） |
| `session.create` 可选模型 | ❌ | 无 modelId/title 入参；模型来自 Host 默认 `ctx.agentDefaultModel`（`apiproxy/src/index.ts:98-100`），创建后经 `session.selectModel` 切换 |
| `skill.list` 入参 sessionId、取 `session.header.cwd`、返回仅 `{name, description, whenToUse?, modelInvocable}`、丢弃 source/provider/rank | ✅ | `api-proxy.ts:3209-3252`；线上 schema 同（`skills.schema.ts:13-18`） |
| 「官方 RpcMethodMap 构建期固定，第三方扩不进去」（D3） | ⚠️ | 点分方法表确为编译期锁定（`rpc-map.ts:24-77`，fetch/handler.ts:74-88）；但 `ctx.connection.rpc` 有运行时扩展点 `intercept` / `handle`（`packages/client/connection/src/rpc.ts:25-53`）；`handle` 注册新通道自动继承信任栅栏与 POST+JSON 强制（`rpc-host.ts:90-115、144-165`）；Typert Remote 也可在 `/api` 上暴露两段式端点（`packages/api/gateway/src/index.ts:104-111`、`docs/api-gateway.md`） |
| `/api` 无协议版本字段；client 与 host 一起发布 | ✅ | 线协议四形态仅 `type/rpcId/method|result/payload`（`api/rpc.ts:150-186`）；头注明示（`api/host.ts:1-4`） |
| 信任栅栏：Host 头 loopback/trustedHosts、POST 强制 application/json、特权方法仅 loopback | ✅ | `packages/client/connection/src/api-request-trust.ts:96-123`（另含 `sec-fetch-site: cross-site` 拒绝与 Origin 一致性检查）；作用域 `/api` 前缀 + 升级路由（`connection/src/index.ts:130-196`）；POST JSON 强制 `fetch/handler.ts:283-286`。**栅栏不覆盖插件经 `webServer.register` 注册的路由**（`packages/host/webserver/src/index.ts:94-101`，"Knows no harness concepts"） |
| 「dsh 无工作区持久层，需自建」（01 §2.2） | ❌ | `@deepseek-ai/dsh-workspace`（`packages/workspace/workspace/`）提供 durable 注册表（canonical path 唯一、排序、sessionIds 会话账号、全局归档集）；web-app bundle 已挂载（`packages/bundle/web-app/cordis.patch.yml:73-74`）；`workspace.*` RPC 7 方法（`api/workspace.ts:38-109`）；05 §4.1 域清单也列了 `workspace.*`，与 01 结论矛盾 |
| `host.pickDirectory` 存在 | ✅ | `api/host.ts:59-62`（OS 原生单目录选择器，仅 native 能力）；实现 `api-proxy.ts:2941-2967`；属特权方法（仅 loopback） |

## 2. 技能系统（01 §2.2、05 §4.7、04 §3.3）

| 主张 | 结论 | 证据 |
|---|---|---|
| 发现根与 rank：`.dsh/skills`(100)、`.agents/skills`(200)、runtime(250)、custom(300)、`~/.dsh/skills`(400)、`~/.agents/skills`(500)、bundled(600) | ✅ | `skill-filesystem/src/index.ts:36-43、241-261`；`skill/src/index.ts:23-27`（runtime 250 / bundled 600） |
| 项目根判定：从 cwd 向上最近 `.git` 祖先，无则回退 cwd | ✅ | `skill-filesystem/src/index.ts:937-947` |
| 扫描格式：每根仅一层 `<root>/<name>/SKILL.md` 或 `<root>/<name>.md`，嵌套不发现 | ✅ | `skill-filesystem/src/index.ts:719-747`（单层 readdir）；watch 侧同（:658-675、`depth: 1` :494） |
| 项目级根仅在 lookup `cwd !== undefined` 时扫描 | ✅ | `skill-filesystem/src/index.ts:243-249` |
| 会话技能经 `tool-skill` 传 `session.header.cwd`，项目技能随会话 cwd 自动生效 | ✅ | `tool-skill/src/index.ts:133、186、222` 三条路径均传会话 cwd |
| 同名覆盖：同层按 rank；跨层近层完全覆盖远层 | ✅ | `skill/src/index.ts:807-811`；`:552-566`（注意"layer"指 Cordis scope 层，非 project/user 根） |
| watcher：Chokidar + fs.watchFile、项目 watcher LRU 128、变更触发 `skills/change` | ✅ | `skill-filesystem/src/index.ts:17、43、310-327、488-502、579-588`；`skill/src/index.ts:649-660` |
| 技能调用 = session.prompt 的 `/name` 约定 | ⚠️ | 上游手势正则扫描用户消息全部 text block，`/name` 是**词边界 token、可在任意位置**（`tool-skill/src/index.ts:409-431`），非仅前缀；仅 `source.kind === 'user'` 消息触发 |
| 上游无「禁用技能」机制（bc 的 `skills-disabled/` 移动是自建） | ✅ | 上游无 skills-disabled/enabled 开关；近似机制是 frontmatter `disable-model-invocation` / `user-invocable: false`（`skill-filesystem/src/index.ts:992-1008`）。移动到子目录因单层扫描确实等效停用 |

## 3. 存储（01 §2.2、05 §4.4、04 §4）

| 主张 | 结论 | 证据 |
|---|---|---|
| `ctx.storage` 是 KV 抽象、json/sqlite 后端可切 | ✅ | `packages/bundle/web-app/cordis.patch.yml:51-62` 装 storage + storage-json + storage-domain（root=`$DSH_HOME/storages`）；后端按名注册可换（`storage/src/registry.ts:25`、`storage-domain/src/index.ts:52-62`） |
| 「document-per-key JSON 文档」（04 §4） | ⚠️ | json 后端是**每 domain 一个 JSON 文件、整域序列化**（`storage-json/src/format.ts:24-35`）；document-per-row 仅 sqlite 后端（`storage-sqlite/src/index.ts:1-6`）。KvTable API：get/put/delete/update/entries/keys/size，快照迭代、无前缀枚举（`storage-domain/src/domain.ts:42-90`） |
| SQLite 仅当前 SCHEMA_VERSION 可开、无迁移 | ✅ | `storage-sqlite/src/schema.ts:20、82-88`（`user_version` 不符即 `version-mismatch`）；json 后端同样仅当前版本（`format.ts:61-67`） |

## 4. 会话持久化与事件（01 §2.2、05 §4.4、04 §6）

| 主张 | 结论 | 证据 |
|---|---|---|
| jsonl 默认 / SQLite 可选 | ✅ | base 装配 jsonl（`packages/bundle/base/cordis.patch.yml:98-102`）；web-app 未覆盖，仓库内无任何组装装 sqlite 后端 |
| `SESSION_FORMAT_VERSION = 0`、无兼容承诺 | ✅ | `packages/core/session/src/types.ts:34-56`；读取端不符即抛（`index.ts:101-102`）；README:143「older: no upgrade path ships yet」 |
| 第三方插件可向会话日志写自定义事件（04 §6「注入写 session 事件」的前提） | ❌（当前不支持） | 读取端未知类型默认拒绝、需 `ignorable: true`（`types.ts:404-436`、`session-persistence/src/coordinator.ts:1063-1064`）；词汇表头注「out-of-repo 插件事件注册面 deferred」（`known-event-types.ts:12-17`）；live `Session.append` 不暴露 ignorable（`core/session/src/index.ts:604-633`）。仅持久化层原始接口 `ctx.sessionPersistence.append` 可带 ignorable（contract 测试用法），非第三方支持面 |
| （评审补充）上游是否已落盘最终 system prompt | ✅（已落盘） | `request/header` 事件 `header.system` = 渲染后完整 system prompt（`types.ts:196-210、304`；写入 `agent-loop/src/agent.ts:337、458-469`，initial/resume/change 快照）。插件经 `ctx.systemPrompt.section()` 注入的段落天然包含在内（`core/system-prompt/src/index.ts:381-455`） |

## 5. Web 扩展机制（01 §2.2、05 §4.2/4.3、03 §2 D1/D2）

| 主张 | 结论 | 证据 |
|---|---|---|
| slot 系统：`ctx.slots.register({name, children?...}, Component)`、shell 只渲染 root | ✅ | `packages/client/ui-slots/src/index.ts:526-550、139-145`；ctx 级仅 root（`runtime/src/client/slots.ts:248-253`）；shell 入口 `client/web/src/app.tsx:41` |
| root 子 slot 名为 sidebar/conversation/details/shell.overlay | ⚠️ | 这 4 个名字是官方 ui-layout 的 root 注册所声明（`ui-layout/src/client/index.ts:116-137`）；children 类型上可声明任何 SlotMap key。占 root 者必须自己重新声明子 slot，否则官方组件注册会抛 "slot is not declared"（`ui-slots/src/index.ts:789-791`）；dispose 级联塌缩子声明（:726-728） |
| 禁用官方 ui-layout 行后自占 root、官方组件保留为子 slot（desktop 高级模式已验证） | ✅ | `demo/deepseek-harness-desktop/dsh-plugin-desktop/src/client/advanced-shell.ts:48-57`（同款 4 子 slot）；其 disable 动作在 launcher profile 代码（`profile.ts:347-361`）。patch 层 `disabled: true` 是官方用法（web-app patch 大量出现） |
| `conversation.chat.node` 渲染器机制存在，可逐块替换 | ✅ | keyed slot，`ui-conversation/src/client/contract/slots.ts:78`、注册 `apply.ts:383`、分发 `ChatNodeSeat.tsx:48`；文档 `docs/cookbook/adding-a-conversation-node.md` |
| 官方主题 CSS 变量 `--dsw-*` 前缀、skin-plugin 演示覆盖 | ✅ | `--dsw-alias-*` 等遍布官方组件 CSS；`demo/skin-plugin/src/client.ts:81-93` 演示覆盖 |
| 官方客户端运行时 React 18.3 | ✅ | 声明 `^18.2.0`（`apps/web/package.json:30` 等），实际解析 18.3.1 |
| `dsh.client` manifest `platform: 'web'` 必填；经典 script 加载 | ✅ | `client/modules/src/index.ts:47-52、115-116、350`；boot 注入 `window.__DSH_BOOT__`（:160-169）、`window.__ModuleLoader__`（`modules/src/client/system.ts:87-88`）、产物为经典 script factory（`tsdown.client.ts:269-271`） |
| webserver：register/registerUpgrade/registerFallback（单座位）/tapIndex | ✅ | `packages/host/webserver/src/index.ts:94-101、109-115、125-131、139-145`。handler 拿原始 `(req, res)`；**无 body 解析/multipart 支持**（全仓库无 busboy/formidable），插件自行实现 |
| patch 替换整行 config（非深合并） | ✅ | `vendor/include/src/index.ts:121-124`；base patch 头注同述 |
| （03 §4 注释暗示）insert 可「在 web-app 层之后」插入 | ⚠️ | `insert` 无位置定位：无 id 时 append 到列表尾、带 id 时推入 group 行 config（`vendor/include/src/index.ts:80-94`）；层序由 patch 来源层序保证，非 insert 语法 |
| `dsh plugin --profile <name> add <pkg>` CLI 存在 | ✅ | `apps/cli/src/bin.ts:40-42`、`apps/cli/src/plugin.ts:120-158`（转发 pnpm + bundle 对账） |

## 6. 调度与作业（01 §2.2、05 §4.6、03 §2 D6、04 §3.4）

| 主张 | 结论 | 证据 |
|---|---|---|
| `cordis-plugin-timer` base 已挂载 | ✅ | `packages/bundle/base/package.json:43`、`cordis.patch.yml:15-17` |
| （隐含）timer 可按 cron 表达式 + timezone 驱动 | ❌ | timer API 仅 timeout/interval/throttle/debounce（毫秒 delay；`vendor/timer/README.md:24-36`、`src/index.ts:12-15`），全文无 cron/timezone。cron 解析/下次触发/时区需自建（建议引 cron-parser 类库） |
| schedule：三工具、after_seconds/at/every_seconds（≥5 分钟）、无 cron、冷 session 无通知、overdue 仅补最新一次 | ✅ | `packages/schedule/schedule/src/domain.ts:24`（MIN=300s）、`tools.ts:282-285`、README:5、111、115。另：schedule 不在默认 web 组合，仅 `examples/web-schedule` overlay 开启 |
| jobs 仅进程内、无跨进程持久化 | ✅ | `packages/jobs/jobs-local/README.md:5、33`；`jobs/README.md:40` |

## 7. 权限与无人值守（04 §3.4、§10.6）

| 主张/问题 | 结论 | 证据 |
|---|---|---|
| 上游无人值守权限接缝（10.6 问询） | ✅（接缝存在） | permission-presets 捆绑 `sandbox/mode` + `approval/policy`，预设创建时 pin、`set(session, name)` 按会话切换（`packages/interaction/permission-presets/README.md`）；`ApprovalPolicy = 'ask' | 'never'`，`'never'` = 每个审批请求自动 resolve `'rejected'`（fail-closed，`user-approval/src/index.ts:90-97、181`）；web 组合默认经 `DSH_PERMISSION_MODE` 接线（`web-app/cordis.patch.yml:168-191`），默认 `workspace-write`+`ask` |

## 8. Windows（02 §3 C7、04 §10.7.4）

| 主张/问题 | 结论 | 证据 |
|---|---|---|
| Windows 上 shell 栈 | ✅（补充细节） | base 按平台切换：win32 默认 pwsh 栈、bash 行禁用（`base/cordis.patch.yml:210-216`）；pwsh 解析链 PS7 安装位置 → PATH → Windows PowerShell 5.1 兜底 → 裸 pwsh（`pwsh-local/src/resolve.ts:21-37`）；**仅 5.1 时非 ASCII stdin 可能误解码**（`pwsh-local/README.md:55`）——中文产品风险 |
| Windows 沙箱机制 | ✅（补充边界） | ACL 受限令牌 runner，保证级别 partial（Everyone 写边界、NTFS 硬链接逃逸、部分 CIM cmdlet 不可用；`packages/sandbox/sandbox-local/src/index.ts:159-166、186-187`、`sandbox-windows-acl/README.md:45-81`）。landlock Linux-only（`native/landlock-run/docs/support-matrix.md`，win32 明确不支持、fail closed） |
| node-pty / Electron 打包风险（R3） | ✅（补充关键细节） | node-pty 是 `dsh-subprocess-local` 硬依赖但 web 默认 preset 不挂 PTY；其 asar 适配是**上游 workspace 的 pnpm patch**（`pnpm-workspace.yaml:72`）——bc 独立 repo 必须自带同等 patch；demo 解法：`npmRebuild=false`（`demo/.../scripts/package-win.ts:147`）+ prebuilds 校验（`verify-packaged-runtime.ts:81-85`）+ asar.unpacked；Electron 下 ACL runner 需 `ELECTRON_RUN_AS_NODE` trampoline、有控制台窗口闪烁坑（`.agents/notes/implemented/architecture/2026-08-15-windows-electron-acl-runner.md`） |
| 上游 Windows 支持成熟度（10.7.4） | ✅ | CI 有 Wine 阻断 job + 原生 Windows 完整门禁（`windows-native`，`.github/workflows/ci.yml:447-454`）；README 无平台声明，支持状态由 CI 与 Agent Notes 体现 |
