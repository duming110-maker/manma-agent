# 任务看板

状态：`待开始` / `进行中` / `待验收` / `通过` / `返工`。派工遵循 `.agents/skills/bc-agent-pm/SKILL.md`。

## P0 可行性 spike

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P0-0 | 拉 `@deepseek-ai/dsh-*@0.1.0-rc.7`，`pnpm install` 跑通，确认桌面壳能起 dsh Host（npm 已确认可拉取） | `pnpm install` 成功；`apps/desktop` 起 dsh Host | 无 | 通过 |
| P0-1 | 修复 frontend-user 构建（补 `utils.ts`、清 `.next/`、加 typecheck/lint） | `pnpm --filter frontend-user build` 通过 | 无 | 通过 |
| P0-2 | 最小布局插件：禁用 `ui-layout` → 占 `root` slot → 自研侧栏 + 官方 conversation 子 slot；产出 slot 去向清单 + 可覆盖/不可覆盖 CSS 变量清单 | `dsh web` + patch 后外壳可见、对话区可收发 | P0-0 | 通过 |
| P0-3 | `connection.rpc.handle('/ext')` 通道验证（继承栅栏 + 与 `/api` 并存） | S3：非 loopback / cross-site 被拒 | P0-0 | 通过 |
| P0-4 | 工作区机制确认：`session.create({workspaceId})` → cwd → `.agents/skills` 技能出现在 `skill.list` | S6 | P0-0 | 通过 |
| P0-5 | 打包形态验证：client 插件构建产物在官方 loader 下运行 | 产物被 `window.__ModuleLoader__` 加载 | P0-2 | 通过 |

## P1 桌面壳

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P1-0 | Electron 精简壳 + NSIS 安装包（内装官方 UI 兼容模式） | 全新 Windows 机器安装即用；loopback 强制；卸载干净 | P0 全通过 | 未拆解（**v2 排序调整：P2 提前，P1 与 P2 并行/后置**——bc-web-ui 是浏览器插件，不依赖 Electron；2026-08-18 PM 据 P0 收官 + 用户反馈调整） |

## P2 外壳移植（提前启动，2026-08-18）

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P2-a | bc-web-ui 真实外壳骨架：替换占位侧栏 → frontend-user 布局结构（侧栏分区/三列 grid/浮层）+ CSS 令牌体系移植（暖灰 + 亮暗双主题 + `bc-agent-theme` 持久化）；导航项渲染空态 | 浏览器可见真实侧栏骨架；亮暗切换生效；官方对话区不受扰 | P0-2 | 通过 |
| P2-b | 会话列表 + 工作区分组（官方 `/api`：workspace.list / session 投影；adapters/upstream.ts 单点） | 侧栏列真实工作区与会话；切换/重命名/归档可用 | P2-a | 通过 |
| P2-c | 新建任务欢迎页（工作区下拉 + 输入区 → `session.create({workspaceId})` → 进会话） | 不选工作区不进会话（D7 硬约束）；全流程可用 | P2-b | 通过 |
| P2-d | 会话页：官方 conversation 嵌入 + 顶部会话头 | GUI 收发链路通（有凭证时真对话） | P2-c | 通过 |
| P2-e | 技能页/定时任务页/设置页骨架（/ext，P3 插件跟上后接真数据） | 页面可达、空态清晰 | P2-a | 通过 |
| P2-f | 文案抽取 zh/en（F10）+ 品牌接入 branding/（铁律 4/5） | S10 双语切换无残留 | P2-a~e | 进行中 |
| P2-0 | （总卡）frontend-user 全量移植为 bc-web-ui（不含库页/知识库 tab） | 页面骨架上线；会话全流程；S10 | 上述子卡 | 未开始 |

## P3 业务插件

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P3a | capability-krm（规则 + 记忆 + 注入链路） | S4 注入可重建 | P2 | 未拆解 |
| P3b | capability-skillx + capability-workspace 薄层收尾 | 技能安装/启停/统计可用 | P2 | 未拆解 |

## P4 定时任务

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P4-0 | capability-cron（cron-parser 调度 + 并发 + 保留策略） | S5 cron 全链路 | P3 | 未拆解 |

## P5 发布

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P5-0 | 官方 settings 内嵌完善、自动更新、备份恢复、开源合规、升级演练 | 02-goals §4 发布标准全项 | P4 | 未拆解 |
