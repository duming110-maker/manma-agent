# 任务看板

状态：`待开始` / `进行中` / `待验收` / `通过` / `返工`。派工遵循 `.agents/skills/bc-agent-pm/SKILL.md`。

> **v0.9（2026-08-19）刷新**：P0/P2 实际状态已按代码核对（HEAD `d13d8b2`）；P3a/P4 已拆解为可执行卡。规划细节以 04-spec 为准，本板是执行视图。开发前先读 `docs/README.md` 的「现状与规划对照」。

## P0 可行性 spike —— 全部通过

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| P0-0 | 拉 `@deepseek-ai/dsh-*@0.1.0-rc.7`，`pnpm install` 跑通，桌面壳能起 dsh Host | `pnpm install` 成功；`apps/desktop` 起 dsh Host | 通过 |
| P0-1 | 修复 frontend-user 构建 | `pnpm --filter frontend-user build` 通过 | 通过 |
| P0-2 | 最小布局插件 + slot 去向/样式清单 | 外壳可见、对话区可收发；清单落档 `docs/spike/P0-2` | 通过 |
| P0-3 | `connection.rpc.handle('/ext')` 通道验证 | S3：非 loopback / cross-site 被拒 | 通过 |
| P0-4 | 工作区机制确认（`session.create({workspaceId})` → cwd → 技能） | S6 | 通过 |
| P0-5 | 打包形态验证（client 产物在官方 loader 下运行） | `window.__ModuleLoader__` 加载；契约 C1–C9 落档 `docs/spike/P0-5` | 通过 |

## P2 外壳移植 —— 主体通过，P2-f 收尾

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P2-a | bc-web-ui 外壳骨架（侧栏分区/三列 grid/令牌体系移植） | 真实侧栏骨架；亮暗切换生效 | P0-2 | 通过 |
| P2-b | 会话列表 + 工作区分组 → **P1-4 修订**：嵌官方 ui-workspace 浏览器（自研 SessionList.tsx 已删） | 侧栏列真实工作区与会话 | P2-a | 通过 |
| P2-c | 新建任务欢迎页 → **P1-4 修订**：官方 hero + `startSession`（自研 WelcomeView.tsx 已删） | 不选工作区不进会话（D7） | P2-b | 通过 |
| P2-d | 会话页：官方 conversation 嵌入 + 顶部会话头 | 收发链路通 | P2-c | 通过 |
| P2-e | 技能页/定时任务页/设置页（/ext 真数据） | 页面真实数据可用 | P2-a | 通过 |
| P2-f | 文案抽取 zh/en（F10）+ 品牌接入 branding/ | 文案已抽取；**S10 冒烟未落地** | P2-a~e | 进行中 |
| P2-0 | （总卡）外壳 + 会话全流程 | 页面骨架 + 会话全流程；S10 | 上述子卡 | 待验收 |

## P3 业务插件

### P3a capability-krm（规则 + 记忆）—— 全部通过

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P3a-1 | krm 存储：`ctx.storage` 的 `defineDomain`（rules/memories 两表，name+version+两阶段写） | 写读往返一致；storage 版本演练（S8 子集） | P2 | 通过 |
| P3a-2 | 注入链路：`systemPrompt.section()`（规则）+ `context()`（记忆） | **S4**：规则段在 `request/header.header.system` 可重建、记忆段在 runtime-context `user/message` 快照可重建 | P3a-1 | 通过 |
| P3a-3 | 前端：RulesMemorySection 空态 → 规则列表/编辑 + 记忆列表/编辑 | 页面可增删改真实数据 | P3a-2 | 通过 |
| P3a-4 | 新文案双语 + 品牌接入（走 `ctx.locale` + branding） | S10 无残留 | P3a-3 | 通过 |

> P3a 落地详情见 `.agents/notes/implemented/2026-08-20-p3a-0-rules与memory.md`。域名为 `bc_krm`（`UNIT_NAME_RE` 无连字符）；storage 版本演练（S8 完整版）随 P5 升级演练做。
> **追加**（2026-08-20）：记忆加单条启用开关（`memoryRecord.enabled`）+ 记忆总开关（`bc_krm` 域 `global.memoriesEnabled`，关闭后全局与工作区记忆一律不注入）；`/ext krm.memories.state/setState`；UI 记忆 tab 顶部总开关 + 行开关 + 表单开关。**AI 自动沉淀记忆**（docs/04-spec §3.6 的 P5 后置项）提前落地：模型面工具 `bc_write_memory`（`ctx.tools.register`），按总开关**动态注册/注销**（关闭时工具不可见、写入 fail-closed）；标题归一化 + 编辑距离去重、project 类型自动工作区作用域、按类型新鲜度分档。

### P3b skillx 收尾 + workspace 薄层（MVP 已在 capability-core）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| P3b-1 | 技能安装校验：按 `docs/06-skill-standard.md` 校验 frontmatter（name/description） | 无 frontmatter 技能被拒并提示 | 通过 |
| P3b-2 | 启停（移动目录语义）+ useCount 统计 + 七层口径标注 | 可启停；统计以 name 为键跨重装保留 | 待开始 |
| P3b-3 | 首次启动强制选工作区（官方 `pickDirectory`，无默认工作区） | 无工作区不进会话 | 待开始 |

### 技能市场（新增，2026-08-19 落地）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| P3b-4 | 市场清单标准 + GitHub 拉取安装（`skills.market.list/install`） | 清单格式见 `docs/06-skill-standard.md` §6；内置 5 个 MIT 技能；GitHub 拉取链路冒烟通过 | 通过 |

## P4 定时任务（执行引擎已落地 2026-08-19）

### P4-0 capability-cron 执行引擎

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P4-1 | 任务存储迁移：`bc-cron-tasks.json` → `ctx.storage` defineDomain | **注**：暂保持 JSON（单机量级足够），打包期再评估；BOM/单元素容错已加 | P2 | 待开始 |
| P4-2 | 调度器：cron-parser（表达式/时区/下次触发）+ `cordis-plugin-timer` 毫秒载体 | 本地时区数学已验（daily/weekly/interval）；30s tick | 无 | 通过 |
| P4-3 | 到点执行：`session.create({workspaceId})` → 默认模型 → prompt | **S5**：冒烟真起会话 + 记录 success（DeepSeek 默认模型） | P4-2 | 通过 |
| P4-4 | 无人值守权限：`sandbox/mode = workspace-write` + `approval/policy = never`（canonical setter 直设） | 无审批提示直跑；fail-closed | P4-3 | 通过 |
| P4-5 | 执行记录 → 插件自有 `node:sqlite`（D4） | **注**：暂用 `bc-cron-runs.json`（上限 200 条），SQLite 打包期落地 | P4-3 | 待开始 |
| P4-6 | 并发保护（同任务不重叠）+ 最小粒度 5 分钟产品约定 | running Set 防重叠 | P4-3 | 通过 |

> P4-0 落地详情见 `.agents/notes/implemented/2026-08-19-p4-0-cron引擎与技能市场.md`。任务需在 UI 填「执行指令」（prompt），留空回退描述。

## P1 桌面壳（最后做；施工图 = docs/spike/P0-5 契约 C1–C9）

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P1-0 | Electron 精简壳 + NSIS 安装包（loopback 强制、私有运行时、卸载干净） | 全新 Windows 机器安装即用；S7 白标 | P0 全通过 | 通过 |

> **P1-0 落地**（2026-08-20，详情见 `.agents/notes/implemented/2026-08-20-p1-0-desktop打包.md`）：Electron 壳（spawn dsh 子进程 + per-brand DSH_HOME + ELECTRON_RUN_AS_NODE）+ NSIS 安装包（`BC-Agent-<ver>-x64-Setup.exe`）+ 插件 tarball 分发（首启 `dsh plugin add`）+ web-ui sourcemap（C9）+ 白标（branding.yaml desktop section）。核心「安装即用」已验（packaged 首启：dsh web + 页面 + tarball 插件 + sourcemap 200）；NSIS 交互安装本机 UAC 无法自动化，真机验证与自动更新/托盘增强/PS7 检测留 P5。

## P5 发布（打包后）

| # | 任务 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|
| P5-0 | 冒烟脚本 S1–S10 落地 + 备份恢复 + 自动更新 + 开源合规 + 升级演练 | 02-goals §4 发布标准全项 | P4 | 未拆解 |
